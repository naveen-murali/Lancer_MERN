export enum MessageTypes {
    TEXT = "text",
    FILE = "file",
    NEGOTIATE = "negotiate",
    PACKEGE = "package"
}

export enum AcceptStatus {
    PENDING = "pending",
    REJECTED = "rejected",
    ACCEPTED = "accepted",
}